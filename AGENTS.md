<safety>
- NEVER display, embed, generate, fetch, decode, inspect, or otherwise expose Scott to an image of the target animal.
- This prohibition includes thumbnails, test fixtures, screenshots, model datasets, browser output, tool output, external links with previews, and accidental failure states.
- NEVER name or describe the target animal, related animals, or any image class in user-visible prose. Refer only to neutral evaluation labels such as `target`, `lookalike`, and `ordinary negative`.
- NEVER place, embed, link to, or otherwise introduce target-animal imagery on any GitHub-related page, including issues, pull requests, discussions, wiki articles, comments, and other repository content.
- Target-animal detection work MUST use a harmless proxy class for every human-visible test, demo, and verification path.
- Project tooling MUST NOT acquire, generate, copy, process, or persist target-animal imagery unless Scott first reviews a concrete plan covering display prevention, isolation, storage locations, retention, and cleanup, then gives explicit permission. Firefox's browser-managed HTTP cache from ordinary browsing remains permitted. Even with approval, imagery MUST NEVER enter host project directories, host temporary directories, anything under `$HOME`, or bind mounts.
- NEVER pass a suspected target-animal image, image URL, or image-bearing file to the harness `read` tool; it decodes and displays images directly in Scott's terminal.
- The auto-discovered `.omp/extensions/image-guard.ts` extension MUST automatically start and verify the stateful `task image-guard` watcher before each project session becomes usable; do not launch or manage a second guard manually. Blocked or failed states deliver hidden cleanup or repair nags while leaving every tool unrestricted. If the watcher reports a path, NEVER inspect, read, or display that exact path, even after the watcher later clears it; unless Scott explicitly directs otherwise, use the preserved tool access to make cleanup the top priority, then resume the interrupted task after the guard clears.
- If any action could cause target-animal imagery to become visible, stop and choose a different method.
</safety>

<workflow>
- Use quick_task agents to write code. They're quick, they're pretty good, but check their work.
- All model evaluation wiki reports MUST follow `.wiki/MobileNetV2.md` as the structure and readability template while preserving model-specific metrics and details.
</workflow>
